// import { createAsyncThunk } from "@reduxjs/toolkit";
// import { getCurrentOrdersRequest } from '../../utils/api';

// export const getCurrentOrder = createAsyncThunk(
//     "getCurrentOrder",
//      async (id) => {
//         const currentOrder = await getCurrentOrdersRequest(id);
//         return { currentOrder };
//     }
// );